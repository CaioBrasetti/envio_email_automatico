<%= form_with(model: niver) do |form| %>
  <% if niver.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(niver.errors.count, "error") %> prohibited this niver from being saved:</h2>

      <ul>
        <% niver.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :data_de_nascimento, style: "display: block" %>
    <%= form.date_field :data_de_nascimento %>
  </div> 

  <div>
    <%= form.submit %>
  </div>
<% end %>
